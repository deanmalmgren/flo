# This file contains all python package dependencies that are needed
# to run all of the examples. If you add an example that requires
# additional python packages to work properly, add them here.

textblob
Pillow
beautifulsoup4

# although these are installed in the system dependencies, also need
# to do this here so that they will be installed in the virtual
# environment on travis-ci
numpy>=1.4
matplotlib

# these is used for testing
pep8
fabric

# these are needed for documentation
sphinx
