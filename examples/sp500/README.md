This example was started to demonstrate how one could use the global
variable namespace as a means to name `creates` and `depends`
variables consistently across several different tasks (see issue #33
for details).
